<form method="post" action="{{ path('admin_user_remove', {'id': user.id}) }}" class="total-flex">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <input type="button" class="button-primary" value="Supprimer"></input>
</form>
